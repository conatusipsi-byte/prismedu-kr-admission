# 인수인계 문서 (HANDOVER)

**작성일**: 2026-08-26
**인계**: 개발자 → 방준현 (클라이언트)
**상태**: 개발 완료·정산 완료 → **유지보수 종료**. 이후 운영·개발은 클라이언트가 직접 진행.

이 문서는 개발을 모르는 분도 읽을 수 있게 썼습니다. 모르는 용어가 나오면
[E. Claude로 개발 이어가기](#e-claude로-개발-이어가기)의 방법으로 Claude에게 그대로 물어보면 됩니다.

> **PDF 버전** — 비개발자용으로 두 개의 PDF를 만들어 뒀습니다.
> - `docs/handover/conatus-인수인계서.pdf` (A~D·F·G장)
> - `docs/handover/conatus-Claude-연동-가이드.pdf` (E장)
>
> 원본은 항상 **이 파일 하나**입니다. 내용을 고친 뒤 `node scripts/docs/build-handover-pdf.mjs`
> 를 실행하면 두 PDF가 다시 만들어집니다.

> **정직성 원칙**: 이 문서는 "되는 것"만 적지 않았습니다. **안 되는 것·미완·알려진 한계**를
> [B장](#b-현재-상태--되는-것--안-되는-것)에 전부 적었습니다. 나중에 "듣지 못했다"는 일이 없도록,
> 불리한 내용도 그대로 남깁니다. 이 문서에 **실제 키(비밀번호) 값은 한 줄도 없습니다.**

---

## 목차

- [A. 프로젝트 개요](#a-프로젝트-개요)
- [B. 현재 상태 — 되는 것 / 안 되는 것](#b-현재-상태--되는-것--안-되는-것)
- [C. 학년도 갱신 — 매년 반드시 해야 하는 일](#c-학년도-갱신--매년-반드시-해야-하는-일)
- [D. 인프라·계정·접근 권한](#d-인프라계정접근-권한)
- [E. Claude로 개발 이어가기](#e-claude로-개발-이어가기)
- [F. 운영 가이드](#f-운영-가이드)
- [G. 인수 직후 체크리스트](#g-인수-직후-체크리스트)

---

## A. 프로젝트 개요

### 무슨 서비스인가

**Conatus** ([conatusipsi.com](https://conatusipsi.com)) — 한국 대학 입시 AI 추천 웹 서비스.
학생이 내신·수능 성적과 비교과 활동을 입력하면, 전국 대학 모집요강 데이터를 바탕으로
지원 가능한 학과를 찾아주고 AI가 적합도를 분석해 줍니다. 진학사·대학어디가를 벤치마크했습니다.

- 미국 입시 AI 플랫폼 **prismedu.kr** 의 코드를 상당 부분 재활용해 만들었습니다.
- 자기소개서(자소서) 첨삭은 **의도적으로 제외** — 24학번부터 자소서가 폐지됐기 때문입니다.

### 주요 기능

| 기능 | 경로 | 설명 |
|---|---|---|
| 학과 검색 | `/admissions` | 대학·학과·전형별 모집요강 검색·필터 |
| 학과 상세 | `/admissions/[대학]/[학과]` | 모집인원·전형방법·수능최저·반영비율·전년도 입결 |
| 합격률 분석 | `/analysis` | 성적 입력 → 학과별 안정/적정/소신 분류 + 합격률 추정 |
| AI 적합도 분석 | 학과 상세 내 | 학생 프로필 vs 학과 요구 → 강/중/약 부합 (Pro) |
| 생기부 AI 분석 | `/spec-analysis` | 학생부 5축 정성 분석 (Pro) |
| AI 입시 카운슬러 | `/chat` | 프로필 기반 실시간 상담 |
| What-If·플래너·비교 | `/what-if` `/planner` `/compare` | 성적 가정 시뮬레이션 등 (Pro) |
| 재외국민·외국인 | `/admissions/jaeoegukmin` | 일반 분석과 분리된 별도 트랙 |
| 컨설팅 예약 | `/consulting` | 슬롯 예약 + 관리자 승인 |
| 문의 | `/inquiries` | 문의 접수 → 관리자 이메일 알림 → 답변 발송 |
| 결제 | `/pricing` `/payment` | 토스페이먼츠 단건결제 (⚠️ B장 참조 — **현재 미작동**) |
| 운영자 | `/admin/*` | 문의·컨설팅·주문·사용자·ETL 관리 |

### 기술 스택 (한 줄 설명 포함)

| 영역 | 기술 | 쉬운 설명 |
|---|---|---|
| 웹 프레임워크 | Next.js 15 / React 19 / TypeScript | 화면과 서버를 같이 만드는 도구 |
| 디자인 | Tailwind CSS + shadcn/ui | 화면 스타일 |
| DB·로그인·파일 | **Supabase** (PostgreSQL) | 데이터 보관·회원 로그인·파일 저장 |
| AI | Anthropic Claude API | AI 분석·상담 (호출당 과금) |
| 결제 | 토스페이먼츠 | 카드 결제 |
| 이메일 | Resend | 가입 인증·문의 답변 메일 |
| 에러 추적 | Sentry | 장애가 나면 자동으로 기록 |
| 배포 | Vercel (서울 리전 icn1) | 실제 서비스가 돌아가는 서버 |
| 테스트 | Vitest (단위) + Playwright (E2E) | 코드가 망가졌는지 자동 검사 |

> 참고: 2026년 5월에 **Firebase → Supabase 마이그레이션**을 완료했습니다.
> 오래된 문서·주석에 firebase 라는 단어가 남아 있을 수 있는데, **실제로는 쓰지 않습니다.**

---

## B. 현재 상태 — 되는 것 / 안 되는 것

> 아래 수치는 **2026-08-26 프로덕션 DB·라이브 사이트에서 직접 조회한 실측값**입니다.

### ✅ 정상 작동 중

#### 1. 학과 검색·모집요강 조회 (핵심 데이터)

| 항목 | 실측값 (2026-08-26 프로덕션 DB) |
|---|---|
| 2027학년도 학과 데이터 **전체** | **93개 대학 / 4,173 학과행** |
| ├ **수시** 전형 보유 | **93개 대학 / 4,170 학과행** |
| └ **정시** 전형 보유 (예정안) | **16개 대학 / 617 학과행** |
| 전년도 공시 입결 보유 | **80개 대학 / 2,822 학과** |
| 대학 마스터 | 321개교 |
| 학과 마스터 | 29,110개 |
| 고등학교 검색 데이터 | 2,396개교 |
| 2026·2028학년도 데이터 | **0행** (C장 참조) |

> 정시 617행은 수시 4,170행과 **별개의 행이 아니라 같은 학과행 안에 함께 들어 있습니다.**
> (한 학과행이 수시·정시 전형을 모두 담습니다. 그래서 전체가 4,173행입니다.)

모집요강 데이터(전형·모집인원·수능최저·반영비율)는 **각 대학 입학처 공식 PDF**에서
추출한 실데이터입니다. 추정·임의생성 데이터는 없습니다.

#### 2. 수능최저학력기준 충족 판정
학생이 입력한 수능 등급으로 "이 전형의 수능최저를 충족하는가"를 **사실 판정**합니다.
확률이 아니라 요강 조건과의 단순 대조라 정확합니다. 조건이 복잡·모호해 자동 판정이
불가능한 전형은 `자동 판정 불가` 로 분류하고 **원문을 그대로 보여줍니다** (지어내지 않음).

#### 3. AI 적합도 분석 (`/api/admission-fit`)
학생 프로필 vs 해당 학과·전형의 요구사항을 비교해 **강/중/약 부합** + 강점·약점·보강 추천.
합격 "확률"이 아니라 정성 평가입니다. 학생이 특정 학과에 대해 명시적으로 요청할 때만
호출됩니다(검색·목록에서는 AI 호출 0회 → 비용 통제).

#### 4. 생기부 AI 5축 분석 (`/spec-analysis`)
학생부 전체 텍스트 → 학생부종합전형 5축 루브릭 정성 분석.
근거 없는 영역은 `정보 부족`(null)으로 두고 점수를 지어내지 않습니다.
생기부 원문은 **서버에 저장하지 않습니다**(민감정보. 캐시에는 해시값과 분석 결과만 저장).

#### 5. AI 입시 카운슬러 챗 (`/chat`)
프로필 기반 상담. 임의 합격선 수치를 만들어내지 못하도록 프롬프트·후처리에서 차단.

#### 6. 컨설팅 예약 / 문의
- 문의 접수 → **운영자에게 이메일 자동 알림**(Resend) → 관리자 페이지에서 답변 발송(이메일).
- 컨설팅 슬롯 생성·일괄생성·예약 승인 (`/admin/consulting/slots`).

#### 7. 농어촌·특성화고 자격 입력 + 전형 필터
자격 조건 입력 → 해당 특별전형만 필터링.

#### 8. 합격률(%) 표시 — **현재 켜져 있음** ⚠️ 조건부
아래 [미완·제한 §1](#1-합격률--켜져-있으나-합격자-표본이-아닌-작년-입결-기반-추정)에 반드시 함께 읽어야 할
중요한 단서가 있습니다.

---

### ⚠️ 미완 · 제한 · 알려진 문제

#### 1. 합격률 — 켜져 있으나, "합격자 표본"이 아닌 **작년 입결 기반 추정**

**현재 프로덕션 상태: 켜짐(ON)**
Vercel 환경변수 `NEXT_PUBLIC_ADMISSION_PROBABILITY_ENABLED=true` 가 설정돼 있고,
라이브 사이트에서 입결·경쟁률·합격률이 노출되는 것을 확인했습니다.

**무엇을 근거로 계산하나 — 그리고 무엇이 없나**

| 근거 | 상태 | 설명 |
|---|---|---|
| 대학 공시 **전년도 입결**(컷 등급·경쟁률) | ✅ 있음 — 80교 2,822학과 | 각 대학 입학처가 공식 발표한 입시결과. 합법 수집. |
| **개별 합격자 표본** (진학사식 데이터) | ❌ **0건** | `admission_sample_stats` 테이블 **0행**. |

즉 현재 표시되는 합격률은 **"작년 합격 컷 등급과 내 내신의 차이"로 계산한 추정치**이며,
실제 합격자 사례 데이터를 기반으로 한 것이 **아닙니다**. 이 사실은 UI에서도 정직하게 표기됩니다:
- 카드에 **"추정" 배지** 노출
- "합격 사례 N건" 대신 **"작년 대학 공시 입결 기반 추정(합격자 표본 아님)"** 문구
- 추정 불확실성 범위 ±12%p 표시

**왜 개별 합격자 표본이 없나**: 학과별 합격자 개인 성적 데이터는 무료 공개 데이터에 없습니다.
진학사·대학어디가가 독점적으로 보유하며, 무단 크롤링은 법적 리스크가 큽니다.
정식 제휴 또는 자체 수집(사용자 제보) 외에는 확보 경로가 없습니다.

**끄고 싶다면**: Vercel → 프로젝트 → Settings → Environment Variables →
`NEXT_PUBLIC_ADMISSION_PROBABILITY_ENABLED` 를 삭제하거나 `false` 로 변경 → **재배포**.
끄면 합격률 자리에 "합격률 분석 데이터 준비 중" 안내가 나오고, 모집요강 정보는 그대로 보입니다.

**정확도를 올리려면 (실제 합격 사례 확보)**:
크라우드소싱 인프라는 **DB·집계 로직까지 이미 만들어져 있습니다**
(`admission_results` 테이블에 제보 적재 → 운영자 검수 → `admission_sample_stats` 집계 →
**검증된 합격 사례 5건 이상 + 가중 표본 3.0 이상**이면 표본 기반 합격률로 자동 전환).
다만 **학생이 제보를 입력하는 화면(UI)은 미구현**입니다.
→ 수능 후(12월~2월)에 제보 폼을 만들어 데이터를 모으는 것이 다음 단계입니다.

#### 2. 결제(토스페이먼츠) — **프로덕션에서 작동하지 않음**

코드는 완성돼 있으나 **키가 등록돼 있지 않아 실제 결제가 불가능**합니다.

```
https://conatusipsi.com/api/health  →  "toss": { "set": false }
                                       "결제 페이지는 503 (출시 직전 등록)"
```

**작동시키려면 순서대로**:
1. 사업자등록 + **통신판매업 신고** (전자상거래법상 의무)
2. 토스페이먼츠 가맹점 가입 → 실키(`TOSS_SECRET_KEY`, `NEXT_PUBLIC_TOSS_CLIENT_KEY`) 발급
3. Vercel 환경변수에 등록 (production/preview/development **3개 타깃 모두**)
4. `NEXT_PUBLIC_BIZ_*` 사업자 정보 입력 — **현재 미입력이라 사이트 하단(footer)에 placeholder 노출 중**
   (`/api/health` → `"business": { "complete": false }`). 통신판매업자 정보 표시는 법적 의무입니다.
5. 재배포 후 소액 실결제 → 환불 테스트

**연쇄 영향 (중요)**: 결제가 막혀 있으므로 **Pro 전용 기능을 일반 사용자가 이용할 방법이 현재 없습니다.**
Pro 전용: 생기부 분석 / AI 적합도 / What-If / 플래너 / 학과 비교 / 무제한 AI 챗.
임시로 열려면 `MASTER_EMAILS` 환경변수에 이메일을 넣거나(운영자 무제한), DB `user_entitlements`
테이블에 직접 권한을 넣어야 합니다.

#### 3. 카카오 알림톡 — 미구현
문의 답변·예약 알림은 **이메일(Resend)로만** 나갑니다. 알림톡은 코드에 자리(placeholder)만
있고 실제 발송 기능은 없습니다. 구현하려면 **카카오 비즈니스 채널 개설 + 템플릿 심사 통과**가
선행돼야 합니다(심사에 보통 며칠~2주). 코드 위치: `lib/notify/inquiry-response.ts`.

#### 4. 모집요강 ETL — 93개 대학 완료, 7개 대학 미적재

목표 100개교 중 93개교 적재. 남은 7개교와 사유:

| 대학 | 사유 | 필요 조치 |
|---|---|---|
| 초당대 | 2027 모집인원표가 **이미지**라 텍스트 추출 불가 | OCR |
| 경일대 | 이미지 PDF | OCR |
| 고신대 | 이미지 PDF (폰트 정보 없어 99.7% 깨짐) | OCR |
| 나사렛대 | 이미지 PDF | OCR |
| 광주대 | 요강은 발표됐으나 입학처 서버가 개발 네트워크에서 **접속 차단** | 다른 네트워크에서 재시도 |
| 서원대 | 동일 (접속 차단) | 다른 네트워크에서 재시도 |
| 세한대 | 요강 발표 여부 미확인 + 접속 차단 | 발표 확인 후 재시도 |

**추가로 알아둘 것**:
- **충남대** — 업로드된 PDF가 파일명만 2027이고 내용은 2026학년도라 **의도적으로 제외**. 2027 요강 재수급 필요.
- **아주대** — 업로드 PDF가 모집요강이 아닌 안내 브로셔라 제외. 요강 재수급 필요.
- **서울과기대** — PDF가 "(안)" = **초안**. `meta.draft=true` 로 기록됨. 확정본으로 교체 권장.
- **중앙대** — 정원외/기회균형 전형의 인원 합계가 원문과 안 맞아 한 차례 거부됐다가 재정합 후 적재됨.

#### 5. 정시 데이터 — 확정본이 아닌 **예정안**
현재 정시 데이터(16개교 617행)는 각 대학이 1년 전 발표한 **대학입학전형시행계획(예정안)** 기준이며,
데이터에 `planStatus="preliminary"` 로 표시돼 있습니다.
**12월 초 정시모집요강 확정본이 나오면 반드시 교체해야 합니다.**
변환표준점수표 등 세부 환산 정보는 12월에나 공개되므로 현재 `not_applicable` 로 비워 뒀습니다
(지어내지 않음).

#### 6. E2E(브라우저 자동 테스트) 비활성
`.github/workflows/e2e.yml` 이 2026-05-21부터 **비활성** 상태입니다.
Firebase → Supabase 마이그레이션 때 끄고 아직 복구하지 않았습니다.
단위 테스트(Vitest)와 타입·빌드 검사는 정상 동작하며 main 푸시마다 자동 실행됩니다.

#### 7. 구글 로그인 비활성
로그인은 **카카오 + 이메일**만 됩니다. 구글은 `app/login/LoginView.tsx` 의
`GOOGLE_OAUTH_ENABLED = false` 로 꺼져 있습니다.
켜려면 Google Cloud Console에서 OAuth 클라이언트 발급 → Supabase Providers에서 Google 활성화 →
이 상수를 `true` 로 변경.

#### 8. 이미 고쳐진 이슈 (참고 — 지금은 정상)
- **고교 검색 결과 0건** → 해결됨. 원인은 NEIS 공공 API가 Vercel 서버 IP를 차단한 것.
  현재는 `high_schools` 테이블(2,396교)에서 직접 조회하도록 바꿔 정상 동작합니다.
- **요금제 가격 표시 잘림**(`₩300,0`) → 해결됨. 글자 크기를 반응형으로 조정.

#### 9. 오래된 내부 문서 주의
`docs/` 폴더에는 개발 과정의 기록이 많이 남아 있는데, **일부는 Firebase 시절 기준이라 현재와 다릅니다.**
- `docs/CURRENT_STATE.md` — 2026-05-09 기준. **오래됨. 이 HANDOVER.md 를 우선하세요.**
- `docs/operations.md` — 시즌 운영 절차는 유효하나 Firestore 용어(컬렉션·트리거)가 남아 있음
- `docs/schema.md` — 일부 Firestore 기준
- **신뢰해도 되는 것**: `CLAUDE.md`, `docs/policy.md`(정책 P-001~P-013), `.env.local.example`, 이 문서

#### 10. ☠️ 절대 실행하면 안 되는 스크립트 — `scripts/seed-sample-data.ts`

개발 초기에 화면 시연용으로 만든 **가짜 데이터 생성 스크립트**입니다.
경쟁률·합격 컷·합격 표본 수를 **난수(Math.random)로 만들어 DB에 넣습니다.**

과거 이 스크립트가 만든 가짜 수치가 학생에게 합격률로 노출될 뻔해서,
2026-06에 전량 삭제하고 기능 자체를 껐던 이력이 있습니다. 지금 DB는 깨끗한 상태입니다
(`admission_sample_stats` 0행, 입결은 대학 공시 데이터만).

**이걸 실행하면 가짜 합격률이 다시 서비스에 노출됩니다.** 파일을 지우거나, Claude에게
*"seed-sample-data.ts 는 절대 실행하지 마"* 라고 못 박아 두세요.
(같은 취지가 `CLAUDE.md` 에도 규칙으로 적혀 있습니다.)

#### 11. Vercel에 남은 Firebase 환경변수 (정리 권장)
`FIREBASE_ADMIN_*`, `NEXT_PUBLIC_FIREBASE_*`, `NEXT_PUBLIC_USE_EMULATOR` 등 8개가 남아 있으나
**현재 코드는 사용하지 않습니다.** 지워도 무방하며, 정리하면 혼선이 줄어듭니다.
(삭제 전 한 번 재배포해 정상 동작 확인 권장)

---

## C. 학년도 갱신 — 매년 반드시 해야 하는 일

> 이 장은 **이 문서에서 가장 중요합니다.** 여기를 놓치면 어느 날 갑자기
> **학과 검색·추천·비교·플래너가 전부 "결과 없음"** 이 됩니다. 화면은 멀쩡하고 에러도
> 안 나기 때문에 원인을 찾기가 매우 어렵습니다.

### C-1. 지금 상태

| 항목 | 실측값 (2026-08-26) |
|---|---|
| DB에 적재된 학년도 | **2027학년도 뿐** |
| 2026학년도 데이터 | 0행 |
| 2028학년도 데이터 | 0행 |

사이트는 "지금이 몇 학년도인가"를 스스로 계산해서 그 학년도 데이터를 DB에서 찾습니다.
**계산 결과가 DB에 없는 학년도면 결과가 0건**이 됩니다.

### C-2. 학년도는 어떻게 정해지나 (우선순위 3단계)

`lib/admission/current-year.ts` 한 곳에서만 결정합니다.

1. **환경변수 `NEXT_PUBLIC_ADMISSION_YEAR`** — 값이 있으면 무조건 이 값 (운영 스위치)
2. 없으면 **시즌 자동 계산** — 매년 **3월**에 다음 학년도로 넘어감
   (1~2월은 직전 학년도 정시가 진행 중이므로 넘기지 않음, 한국 시간 기준)
3. 마지막으로 **`LATEST_LOADED_ADMISSION_YEAR`(현재 2027) 상한으로 잘라냄**
   → 아직 데이터를 넣지 않은 미래 학년도는 **절대 조회하지 않음**

> **왜 이렇게 만들었나 (2026-08-26 수정 이력)**
> 그 전 코드는 10곳에서 각자 `올해 + 1` 로 계산했습니다. 이 식은 **1월 1일에 넘어가므로**
> **2027년 1월 1일 0시부터 2028학년도를 조회** — DB에 2028 데이터가 없으니 검색·추천·비교·
> 플래너·학과 상세가 **전부 빈 결과**가 될 상황이었습니다. 테스트도 같은 식을 쓰고 있어
> 자동으로 걸러지지 않았습니다. 이관 점검에서 발견해 위 3단계 방식으로 바꿨고,
> 이제 상한 클램프 덕분에 **최악의 경우에도 직전 학년도가 보일 뿐 빈 사이트가 되지 않습니다.**
>
> (이 수정은 **2026-08-26 작업분**입니다. `main` 에 반영·배포된 뒤부터 적용됩니다.)

### C-3. 2028학년도 데이터를 넣을 때 (매년 7~9월)

**세 가지를 다 해야 합니다. 하나라도 빠지면 검색이 비어 보입니다.**

| 순서 | 할 일 | 안 하면 |
|---|---|---|
| ① | 각 대학 2028 모집요강 PDF 수집 → **ETL 적재** (E-4 예시 프롬프트 ④) | 데이터 자체가 없음 |
| ② | `lib/admission/current-year.ts` 의 **`LATEST_LOADED_ADMISSION_YEAR` 를 2028 로** 올리기 | 상한에 걸려 계속 2027만 조회 |
| ③ | Vercel 환경변수 **`NEXT_PUBLIC_ADMISSION_YEAR=2028`** 로 변경 후 재배포 | 값이 남아 있으면 계속 2027 조회 |

Claude에게는 이렇게 말하면 됩니다 — *"2028학년도 데이터로 갱신해줘. HANDOVER.md C장 절차대로."*

### C-4. 환경변수 등록 시 주의

- `NEXT_PUBLIC_ADMISSION_YEAR` 는 Vercel의 **Production / Preview / Development 3개 타깃 모두**에
  등록하세요. (한 곳만 넣으면 미리보기 배포와 실서비스가 서로 다른 학년도를 봅니다.)
- **2026-08-26 확인: 현재 Vercel에는 이 변수가 등록돼 있지 않습니다.**
  등록돼 있지 않아도 위 2·3단계(시즌 계산 + 상한 클램프) 덕분에 **정상 동작**하지만,
  명시적으로 넣어 두는 편이 안전합니다(`NEXT_PUBLIC_ADMISSION_YEAR=2027`).
- 이 값은 비밀이 아닙니다. `NEXT_PUBLIC_` 접두사가 붙는 게 맞습니다(브라우저도 같은 값을 봐야 함).

### C-5. 갱신 후 확인 방법

브라우저에서 아래 주소를 열어 응답 안의 `"year"` 값이 의도한 학년도인지 확인합니다.
(학과 상세 API. 아래는 한양대 경영학부 예시)

```
https://conatusipsi.com/api/admissions/hanyang/u02010100038-ba-d
   →  { "year": 2027, ... }   ← 이 숫자가 방금 올린 학년도여야 합니다
```

학과 검색(`/admissions`)에서 결과가 정상적으로 나오는지도 함께 확인하세요.

또는 Claude에게 *"지금 사이트가 몇 학년도 데이터를 조회하고 있는지 확인해줘"* 라고 하면
코드와 실제 응답을 함께 확인해 줍니다.

---

## D. 인프라·계정·접근 권한

### D-1. 서비스 목록 (전부 클라이언트 명의 — 명의 이전 불필요)

| 서비스 | 용도 | 식별자 |
|---|---|---|
| **GitHub** | 소스코드 저장소 (private) | `conatusipsi-byte/prismedu-kr-admission` |
| **Vercel** | 서비스 배포·호스팅 | 프로젝트 `prismedu-kr-admission` (팀 `joonhyeon-s-projects`), 리전 `icn1`(서울) |
| **Supabase** | DB·로그인·파일저장 | 프로젝트 ref `bqmccfeglxzzrmgdirxe` |
| **도메인** | conatusipsi.com | Vercel에 연결됨 |
| **Anthropic** | AI (Claude API) | console.anthropic.com — **잔액 소진 시 AI 기능 정지** |
| **Resend** | 이메일 발송 | resend.com |
| **토스페이먼츠** | 결제 | app.tosspayments.com (⚠️ 아직 실키 미등록) |
| **NEIS** | 고교 정보 API | open.neis.go.kr (현재는 DB 적재본 사용, API 의존 없음) |
| **KCUE / 대학알리미** | 대학·학과 기초정보 | data.go.kr 공공데이터포털 |
| **Sentry** | 에러 추적 | sentry.io |
| **카카오 디벨로퍼스** | 카카오 로그인 | developers.kakao.com |

### D-2. 개발자(기존) 접근 권한 제거 방법

인수 후 **바로** 진행하시길 권합니다. 순서대로 하면 됩니다.

1. **GitHub** — 저장소 → `Settings` → `Collaborators and teams` → 개발자 계정 `Remove`.
   Organization 소속이면 `People` 에서도 제거.
2. **Vercel** — 팀 → `Settings` → `Members` → 개발자 계정 `Remove`.
   추가로 `Account Settings` → `Tokens` 에서 **개발자에게 발급했던 토큰을 삭제**(중요).
3. **Supabase** — 프로젝트 → `Settings` → `Team` (또는 Organization → Members) → 개발자 제거.
   `Account` → `Access Tokens` 에서 개발용 Personal Access Token **삭제**
   (중요 — 이 토큰 하나로 DB 전체를 조작할 수 있습니다).
4. **Anthropic Console** — `Settings` → `Members` 에서 제거 + `API Keys` 에서 개발용 키 **폐기(Revoke) 후 새 키 발급**.
5. **Resend** — `Team` 에서 제거 + `API Keys` 재발급.
6. **토스페이먼츠** — 상점 관리자 계정 접근 권한 정리(현재 실키 미발급이라 해당 없을 수 있음).
7. **Sentry** — `Settings` → `Members` 제거.
8. **카카오 디벨로퍼스** — 앱 → `팀 관리` 에서 제거.
9. **도메인 등록기관** — 계정 비밀번호 변경 + 2단계 인증 활성화.

> ### 🔑 키 회전(재발급) 권장
> 개발 중 개발자 PC에 있던 키들은 이관 후 **전부 새로 발급하고 옛 키를 폐기**하는 것이
> 가장 안전합니다. 특히 다음 3개는 노출 시 피해가 큽니다:
> - `SUPABASE_SERVICE_ROLE_KEY` — DB 전체 읽기·쓰기 (권한 검사 우회)
> - `SUPABASE_ACCESS_TOKEN` — 프로젝트 설정·DB 마이그레이션 조작
> - `ANTHROPIC_API_KEY` — 요금이 실제 청구됨
>
> 재발급 순서: 각 서비스 콘솔에서 새 키 발급 → **Vercel 환경변수 교체** → 재배포 → 정상 확인 → 옛 키 폐기.

### D-3. 환경변수 목록 (⚠️ 값은 기재하지 않음)

전체 목록과 발급 방법 설명은 저장소의 **`.env.local.example`** 파일에 있습니다.
실제 값은 **Vercel → Settings → Environment Variables** 에 저장돼 있습니다.

| 환경변수 | 용도 | 필수 | 없으면 |
|---|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | DB 주소 | ✅ | 사이트 전체 동작 불능 |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | 브라우저용 DB 키 (공개돼도 정상) | ✅ | 로그인·조회 불능 |
| `SUPABASE_SERVICE_ROLE_KEY` | **서버 전용** DB 마스터 키 | ✅ | 서버 기능 불능 · **절대 공개 금지** |
| `SUPABASE_ACCESS_TOKEN` | DB 마이그레이션·ETL 스크립트용 | 스크립트만 | ETL 실행 불가 |
| `ANTHROPIC_API_KEY` | AI 분석·챗 | ✅ | AI가 mock(가짜) 응답으로 대체 |
| `NEIS_API_KEY` | 고교 정보 수집 | 선택 | 고교 데이터 재수집 불가 (기존 적재분은 계속 동작) |
| `KCUE_API_KEY` | 대학·학과 기초정보 수집 | 선택 | 대학 기초정보 갱신 불가 |
| `RESEND_API_KEY` | 이메일 발송 | ✅ | 가입 인증메일·문의 알림 안 나감 |
| `TOSS_SECRET_KEY` | 결제 (서버) | 결제 시 | 결제 페이지 503 |
| `NEXT_PUBLIC_TOSS_CLIENT_KEY` | 결제 (브라우저) | 결제 시 | 결제 위젯 안 뜸 |
| `NEXT_PUBLIC_KAKAO_CLIENT_ID` / `KAKAO_CLIENT_SECRET` | 카카오 로그인 | 선택 | 실제 활성화는 Supabase Providers 설정이 좌우 |
| `MASTER_EMAILS` | **운영자 이메일 목록**(쉼표 구분) | ✅ | 관리자 페이지 접근·문의 알림 수신 불가 |
| `NEXT_PUBLIC_ADMISSION_PROBABILITY_ENABLED` | 합격률 기능 on/off | — | 미설정 시 OFF (현재 `true`) |
| `NEXT_PUBLIC_ADMISSION_YEAR` | **조회할 입시 학년도** (C장) | 권장 | 시즌 자동계산 + 적재 상한으로 폴백 (**현재 Vercel 미등록**) |
| `NEXT_PUBLIC_SENTRY_DSN` / `SENTRY_*` | 에러 추적 | 선택 | 에러 자동 기록 안 됨 |
| `NEXT_PUBLIC_GA_ID` | 구글 애널리틱스 | 선택 | 방문 통계 없음 |
| `NEXT_PUBLIC_BIZ_*` | 사업자 정보 (footer 법적 표시) | 결제 시 | footer에 placeholder 노출 (**현재 상태**) |
| `NEXT_PUBLIC_APP_STORE_URL` / `NEXT_PUBLIC_PLAY_STORE_URL` | 앱 다운로드 링크 | 선택 | 링크가 `#` |
| `VERCEL_TOKEN` | CLI 배포용 | 선택 | 명령줄 배포 불가 (웹 대시보드는 가능) |

> **⚠️ 절대 규칙 2가지**
> 1. `NEXT_PUBLIC_` 로 시작하는 변수는 **브라우저에 그대로 노출**됩니다.
>    `SUPABASE_SERVICE_ROLE_KEY`·`TOSS_SECRET_KEY`·`VERCEL_TOKEN` 같은 비밀키에는
>    **절대 `NEXT_PUBLIC_` 을 붙이지 마세요.**
> 2. 새 환경변수를 추가할 때는 Vercel의 **Production / Preview / Development 3개 타깃 모두**에
>    등록하세요. Preview에 Supabase 변수가 빠지면 미리보기 배포에서 로그인이 전부 실패합니다.

### D-4. 배포는 어떻게 되나

**GitHub `main` 브랜치에 코드가 올라가면 Vercel이 자동으로 프로덕션에 배포**합니다.
별도 명령이 필요 없습니다. 배포 상태는 vercel.com 대시보드에서 확인합니다.

수동 배포가 필요하면:
```bash
npx vercel --prod --scope joonhyeon-s-projects
```

---

## E. Claude로 개발 이어가기

이 프로젝트는 **Claude Code** 로 개발됐습니다. 같은 방식으로 이어가면 Claude가 코드를 읽고
직접 수정·테스트·배포까지 해 줍니다. 코딩을 몰라도 **한국어로 지시**하면 됩니다.

### E-1. 설치 (한 번만)

1. **Node.js 설치** — [nodejs.org](https://nodejs.org) 에서 LTS 버전 다운로드 후 설치
2. **Claude Code 설치** — [claude.com/code](https://claude.com/code) 안내에 따라 설치
   (터미널에서 `npm install -g @anthropic-ai/claude-code`)
3. **Claude 계정 로그인** — 터미널에서 `claude` 실행 → 안내에 따라 로그인
   (Claude 구독 또는 Anthropic API 크레딧 필요)

### E-2. 프로젝트 열기

터미널(맥: 터미널 / 윈도우: PowerShell)에서 아래를 순서대로 입력합니다.

```bash
# 1) 코드 내려받기 (최초 1회)
git clone https://github.com/conatusipsi-byte/prismedu-kr-admission.git
cd prismedu-kr-admission

# 2) 필요한 프로그램 설치 (최초 1회, 몇 분 걸림)
npm install

# 3) 환경변수 파일 만들기 (최초 1회)
#    Vercel 대시보드의 환경변수 값을 복사해 .env.local 파일에 붙여넣기.
#    형식은 .env.local.example 파일 참고. 또는 아래 명령으로 자동 내려받기:
npx vercel env pull .env.local --scope joonhyeon-s-projects

# 4) Claude Code 실행
claude
```

이후에는 **`cd prismedu-kr-admission` → `claude`** 두 줄이면 됩니다.

로컬에서 사이트를 직접 띄워보려면 `npm run dev` → 브라우저에서 `http://localhost:9002`.
(로컬 개발 서버도 **실제 원격 Supabase DB에 연결**되니, 데이터 수정 작업은 주의하세요.)

### E-3. Claude가 자동으로 참고하는 규칙 파일

저장소 최상단의 **`CLAUDE.md`** 에 이 프로젝트의 규칙이 정리돼 있습니다.
Claude Code는 **매번 이 파일을 자동으로 읽고** 작업합니다. 예를 들어 이런 것들이 들어 있습니다:

- 기술 스택과 폴더 구조
- 브랜드 색상·디자인 톤
- **정직성 원칙** — 데이터가 없으면 "모른다"고 말하고, 수치를 지어내지 않는다
- **ETL 주의사항** — 데이터 적재 시 정시 데이터가 지워지지 않게 하는 방법
- 진학사·대학어디가 크롤링 금지 등 법적 주의사항

규칙을 바꾸고 싶으면 Claude에게 *"CLAUDE.md에 ○○ 규칙을 추가해줘"* 라고 하면 됩니다.

### E-4. 실전 예시 프롬프트 6개

그대로 복사해서 Claude Code에 붙여넣으면 됩니다.

---

**① 프로젝트 파악하기 (제일 먼저 해볼 것)**
```
이 프로젝트 구조를 설명해줘. HANDOVER.md와 CLAUDE.md를 먼저 읽고,
어떤 기능이 어느 폴더에 있는지, 지금 안 되는 기능이 뭔지 정리해서 알려줘.
```

---

**② 기능 추가하기**
```
학과 검색(/admissions)에 "모집인원 순으로 정렬" 기능을 추가해줘.
기존 필터 UI 패턴과 일관되게 만들고, 작업 끝나면 npm run typecheck와 npm test를 돌려서
통과하는지 확인해줘.
```
> 팁: 기능을 말할 때 **화면 경로(`/admissions`)** 나 **화면에 보이는 글자**를 같이 알려주면
> Claude가 정확한 파일을 훨씬 빨리 찾습니다.

---

**③ 합격률 기능 상태 확인·개선**
```
합격률 기능이 지금 어떤 데이터를 근거로 계산되는지 코드로 확인해서 설명해줘.
"합격자 표본 기반"으로 전환하려면 무엇이 더 필요한지, 학생 제보 입력 UI를 만든다면
어느 파일을 건드려야 하는지 알려줘. HANDOVER.md B장 1번 항목을 참고해.
```

---

**④ 새 대학 모집요강 PDF 적재하기 (ETL)**
```
새 대학 모집요강 PDF를 적재하려고 해. 파일 경로는 ○○○ 이고 대학은 ○○대야.
scripts/etl/ 의 기존 절차(텍스트 추출 → 구조화 JSON → dept-matcher 매칭 → load-admissions)를
그대로 따라줘.

반드시 지킬 것:
- load-admissions.ts 실행 시 --only=<대학ID> 를 꼭 붙일 것 (정시 데이터 덮어쓰기 방지)
- --execute 없이 먼저 dry-run 으로 결과를 보여주고, 내 확인을 받은 뒤에 실행할 것
- 학과 매칭이 애매하면 추정하지 말고 건너뛰고 리포트할 것
- 적재 후 정시 617행이 그대로 남아있는지 검증할 것
```

---

**⑤ 12월 정시 확정본으로 교체하기**
```
정시 데이터를 12월 확정본(정시모집요강)으로 교체하려고 해.
지금 DB에는 예정안(planStatus="preliminary")이 16개 대학 617행 들어있어.

scripts/etl/load-jeongsi-plan.ts 를 읽고, 확정본 교체 절차를 먼저 설명해줘.
- 기존 수시 데이터(93교 4,173행)가 절대 지워지지 않는 방식(JSONB 병합)인지 확인
- planStatus 를 preliminary → finalized 로 바꾸는 지점
- --commit 없이 dry-run 먼저

설명을 듣고 내가 확인한 다음에 실행할게.
```

---

**⑥ 다음 학년도(2028)로 갱신하기 — 매년 필요**
```
2028학년도 데이터로 갱신하려고 해. HANDOVER.md 의 C장(학년도 갱신)을 먼저 읽고
아래 3단계를 순서대로 진행해줘.

1) 2028 모집요강 PDF 를 ETL 로 적재 (load-admissions.ts 는 --only 필수, dry-run 먼저)
2) lib/admission/current-year.ts 의 LATEST_LOADED_ADMISSION_YEAR 를 2028 로 상향
3) Vercel 환경변수 NEXT_PUBLIC_ADMISSION_YEAR 를 2028 로 변경 (3개 타깃 모두) 후 재배포

각 단계마다 내 확인을 받고 진행하고, 끝나면 학과 상세 API 의 "year" 가 2028 로
바뀌었는지, 검색 결과가 비어있지 않은지 실제로 확인해서 보여줘.
```

---

### E-5. ⚠️ Claude에게 작업시킬 때 꼭 지킬 것

1. **DB를 수정하는 작업 전에는 반드시 백업**
   Supabase 대시보드 → `Database` → `Backups` 에서 백업 상태 확인.
   Claude에게 *"작업 전에 백업부터 하자"* 라고 먼저 말하세요.
   데이터를 지우거나 통째로 덮어쓰는 작업은 되돌리기 어렵습니다.

2. **배포 전에는 항상 테스트**
   ```bash
   npm run typecheck   # 타입 오류 검사
   npm test            # 단위 테스트 (2026-08-26 기준 90개 파일 / 980개 테스트 전부 통과)
   npm run build       # 빌드 성공 확인
   ```
   숫자는 기능이 늘면 함께 늘어납니다. 중요한 건 **"실패 0건"** 입니다.
   하나라도 빨간색(FAIL)이면 배포하지 마세요.
   Claude에게 *"배포 전에 typecheck·test·build 다 돌려서 통과하는지 보여줘"* 라고 하면 알아서 합니다.
   `main` 에 푸시하면 **바로 실서비스에 반영**되므로, 확인 없이 푸시하지 않게 하세요.

3. **ETL(데이터 적재) 시 `--only` 를 반드시 사용**
   `scripts/etl/load-admissions.ts` 를 `--only` 없이 `--execute` 하면 **모든 대학의 전형 데이터를
   통째로 다시 씁니다.** 이때 나중에 따로 넣은 **정시 데이터(617행)가 날아갈 수 있습니다.**
   ```bash
   npx tsx scripts/etl/load-admissions.ts --only=<대학ID>            # dry-run (안전, DB 변경 없음)
   npx tsx scripts/etl/load-admissions.ts --only=<대학ID> --execute  # 실제 적재
   ```
   적재 후에는 정시 행 수가 그대로인지 반드시 확인하세요.

4. **AI 기능은 호출할 때마다 돈이 나갑니다**
   [console.anthropic.com](https://console.anthropic.com) → `Usage` / `Billing` 에서 잔액을 주기적으로 확인하세요.
   잔액이 떨어지면 AI 챗·분석이 mock(가짜) 응답으로 대체됩니다.
   Claude Code 자체 사용료와 서비스의 AI 기능 사용료는 **별개**입니다.

5. **모르면 그냥 물어보세요**
   *"방금 한 작업이 무슨 뜻이야?"*, *"이거 위험한 작업이야?"*, *"되돌릴 수 있어?"* —
   Claude는 한국어로 다 설명해 줍니다. 이해가 안 되는 상태로 `main` 푸시나 DB 수정을 승인하지 마세요.

---

## F. 운영 가이드

### F-1. 문의(Inquiry) 처리

- 사용자가 `/inquiries` 에서 문의를 남기면 **`MASTER_EMAILS` 에 등록된 이메일로 자동 알림**이 갑니다.
- 확인·답변: `https://conatusipsi.com/admin/inquiries`
- 답변을 등록하면 사용자에게 **이메일**로 발송됩니다 (알림톡은 미구현 — B장 3번).
- 이메일이 안 나가면: `RESEND_API_KEY` 설정 여부와 `MASTER_EMAILS` 값을 먼저 확인하세요.

### F-2. 컨설팅 슬롯 관리

- 슬롯 생성·일괄 생성·삭제: `https://conatusipsi.com/admin/consulting/slots`
- 예약 신청 확인·승인: `https://conatusipsi.com/admin/consulting`
- 슬롯을 미리 만들어 두지 않으면 사용자 화면에 예약 가능한 시간이 뜨지 않습니다.

### F-3. 운영자(master) 권한

두 가지 경로가 있습니다.

1. **`MASTER_EMAILS` 환경변수** (권장·간단) — Vercel에 쉼표로 구분한 이메일 목록을 넣습니다.
   여기 있는 이메일은 관리자 페이지 접근 + 모든 플랜 제한 우회 + 문의 알림 수신.
   변경 후 **재배포해야 반영**됩니다.
2. **`admins` 테이블** — DB에 `user_id` 와 `active=true` 를 넣으면 관리자가 됩니다.
   (자주 바뀌는 일반 운영자용. 재배포 불필요)

> 보안: `MASTER_EMAILS` 에는 절대 `NEXT_PUBLIC_` 을 붙이지 마세요.
> 붙이면 운영자 이메일 목록이 브라우저에 그대로 노출됩니다.

### F-4. AI 비용

| 기능 | 사용 모델 | 대략 비용(1회) |
|---|---|---|
| 생기부 5축 분석 | Sonnet 계열 | 약 $0.02 ~ 0.03 |
| AI 적합도 분석 | Sonnet 계열 | 약 $0.02 ~ 0.03 |
| AI 챗 (무료 사용자) | Haiku 계열 | 훨씬 저렴 |
| AI 챗 (Pro/Elite) | Sonnet / Opus 계열 | 더 비쌈 |

> 위 금액은 입력 길이에 따라 달라지는 **추정치**입니다. 정확한 지출은
> [console.anthropic.com](https://console.anthropic.com) → `Usage` 에서 확인하세요.

**이미 들어가 있는 비용 방어 장치** (지우지 마세요):
- **캐시**(`ai_cache` 테이블) — 같은 입력의 재요청은 토큰 0으로 즉시 응답
- **호출 제한**(`rate_limits`) — 생기부: 분당 3회 / 하루 20회, 적합도: 분당 5회 / 하루 30회
- **플랜 게이트** — 비싼 분석은 Pro 이상만
- **입력 길이 상한** — 생기부 30,000자
- 검색·목록 화면에서는 AI를 호출하지 않음

사용 모델은 코드에 고정돼 있습니다(`lib/anthropic.ts`, 각 API 라우트의 `MODEL` 상수).
더 저렴하거나 더 좋은 최신 모델로 바꾸고 싶으면 Claude에게
*"AI 모델을 최신 버전으로 바꾸고 비용 영향을 알려줘"* 라고 요청하세요.

### F-5. 데이터 갱신 시기 (연간 캘린더)

| 시기 | 할 일 | 중요도 |
|---|---|---|
| **매년 5~7월** | 다음 학년도 **수시 모집요강** 발표 → PDF 수집·ETL 재적재 | ★★★ 핵심 |
| **적재 직후** | `LATEST_LOADED_ADMISSION_YEAR` 상향 + `NEXT_PUBLIC_ADMISSION_YEAR` 갱신 (C장 · 프롬프트 ⑥) | ★★★ 필수 |
| **7~9월** | 수시 시즌 트래픽 증가 — 에러·응답속도 모니터링 | ★★ |
| **11월 셋째 목요일** | 수능 | — |
| **12월 초** | **정시 확정본 교체** (현재 데이터는 예정안) + 변환표준점수표 반영 | ★★★ 필수 |
| **12월~2월** | 합격 사례 제보 수집 최적기 (제보 UI를 만든다면 이때 맞춰서) | ★★ |
| **1월 말** | 수시→정시 이월 인원 반영 | ★★ |

각 작업은 [E-4의 예시 프롬프트 ④⑤⑥](#e-4-실전-예시-프롬프트-6개)을 그대로 쓰면 됩니다.

### F-6. 문제가 생겼을 때

1. **서비스 상태 한 번에 확인**: 브라우저에서 `https://conatusipsi.com/api/health`
   → 각 외부 서비스 연결 상태를 비밀값 노출 없이 보여줍니다.
2. **에러 로그**: Sentry 대시보드 (자동 기록)
3. **배포 로그**: Vercel 대시보드 → `Deployments`
4. **직전 배포로 되돌리기**: Vercel → `Deployments` → 이전 배포 → `Promote to Production`
   (코드 문제로 사이트가 깨졌을 때 가장 빠른 응급 조치)
5. 원인을 모르겠으면 Claude Code에 그대로 물어보세요:
   *"사이트에서 ○○ 하면 △△ 에러가 나. 원인 찾아서 고쳐줘."*

---

## G. 인수 직후 체크리스트

- [ ] 각 서비스에서 **개발자 접근 권한 제거** (D-2)
- [ ] **주요 키 재발급** — Supabase service_role / Supabase access token / Anthropic / Resend (D-2)
- [ ] Claude Code 설치 + 저장소 열어서 **예시 프롬프트 ①** 실행해 보기 (E-4)
- [ ] `https://conatusipsi.com/api/health` 열어서 현재 상태 확인
- [ ] `MASTER_EMAILS` 에 본인 이메일이 들어 있는지 확인 (관리자 페이지·문의 알림)
- [ ] 합격률 기능을 **켠 채로 둘지 끌지 결정** (B장 1번 — 지금은 켜져 있고 "추정" 표기)
- [ ] 결제를 열 계획이라면: 사업자등록 → 통신판매업 신고 → 토스 실키 등록 → `NEXT_PUBLIC_BIZ_*` 입력 (B장 2번)
- [ ] Vercel에 `NEXT_PUBLIC_ADMISSION_YEAR=2027` 등록 (3개 타깃 모두 — C-4)
- [ ] Supabase 자동 백업이 켜져 있는지 확인
- [ ] 12월 정시 확정본 교체 일정을 캘린더에 등록 (B장 5번)

---

## 마지막으로

- 이 문서는 **2026-08-26 기준 실제 코드·DB·라이브 사이트를 확인해서 작성**했습니다.
- 시간이 지나면 내용이 달라질 수 있습니다. 큰 작업을 한 뒤에는 Claude에게
  *"HANDOVER.md를 지금 상태에 맞게 갱신해줘"* 라고 하면 이 문서를 직접 고쳐 줍니다.
- 판단이 서지 않는 작업(DB 데이터 삭제, 대량 데이터 교체, 결제 설정)은 **실행 전에 Claude에게
  "이거 위험해? 되돌릴 수 있어?" 라고 먼저 물어보는 습관**을 권합니다.

행운을 빕니다. 🎓
